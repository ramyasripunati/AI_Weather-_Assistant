const form = document.querySelector("#weather-form"), cityInput = document.querySelector("#city"), button = form.querySelector("button"), status = document.querySelector("#status"), result = document.querySelector("#result");
const setText = (id, value) => { document.querySelector(id).textContent = value; };
form.addEventListener("submit", async (event) => { event.preventDefault(); const city = cityInput.value.trim(); if (!city) return; button.disabled = true; status.textContent = "Getting current weather…"; result.hidden = true; try { const response = await fetch(`/api/weather?city=${encodeURIComponent(city)}`); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Unable to get weather."); setText("#location", `${data.city}, ${data.country}`); setText("#description", data.description); setText("#temperature", data.temperature); setText("#feels-like", data.feels_like); setText("#humidity", data.humidity); setText("#wind", data.wind_speed); setText("#advice", data.advice); const icon = document.querySelector("#weather-icon"); icon.src = `https://openweathermap.org/img/wn/${data.icon}@2x.png`; icon.alt = data.description; status.textContent = ""; result.hidden = false; } catch (error) { status.textContent = error.message; } finally { button.disabled = false; } });
